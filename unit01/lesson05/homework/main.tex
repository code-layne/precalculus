%! Composition of Functions — Unit 1, Lesson 1.5 — Homework
\documentclass[10pt]{article}
\usepackage{precalculus-article}
\usepackage{precalculus-boxes}
\newcommand{\TallMath}[1]{$\displaystyle #1\rule[-1.4em]{0pt}{3.2em}$}

\begin{document}

\pageheader{Unit 1, Lesson 1.5}{Homework}

\vspace{0.12in}

\begin{enumerate}[itemsep=10pt]

\item The table gives every value of two functions $f$ and $g$. Evaluate each
      composite by working from the inside out.

\smallskip
\renewcommand{\arraystretch}{1.5}
\begin{center}
\begin{tabular}{|c|c|c|c|c|c|}
\hline
$x$ & 1 & 2 & 3 & 4 & 5 \\
\hline
$f(x)$ & 5 & 1 & 4 & 2 & 3 \\
\hline
$g(x)$ & 3 & 5 & 1 & 5 & 2 \\
\hline
\end{tabular}
\end{center}

\begin{enumerate}[label=(\alph*), itemsep=6pt]
  \item $f(g(1)) = $ \blank{2.4cm} \qquad (inner value: \blank{1.4cm})
  \item $g(f(1)) = $ \blank{2.4cm} \qquad (inner value: \blank{1.4cm})
  \item $f(g(3)) = $ \blank{2.4cm} \qquad (inner value: \blank{1.4cm})
  \item $g(g(4)) = $ \blank{2.4cm} \qquad (inner value: \blank{1.4cm})
\end{enumerate}

\item Let $f(x) = x^2 + 1$ and $g(x) = x - 4$. Build both composites and
      simplify completely.

\begin{work}
  f(g(x)) &= f(x - 4) \\
          &= (x - 4)^2 + 1 \\
          &= x^2 - 8x + 16 + 1 \\
          &= x^2 - 8x + 17 \\
  g(f(x)) &= g(x^2 + 1) \\
          &= (x^2 + 1) - 4 \\
          &= x^2 - 3
\end{work}

$(f \circ g)(x) = $ \blank{3.8cm} \qquad $(g \circ f)(x) = $ \blank{3cm}

\item \textbf{Back at the checkout counter.} The \$10 coupon is
      $c(p) = p - 10$ and the 6\% sales tax is $t(x) = 1.06x$. A \$45 shirt
      goes through two different registers.

\begin{work}
  t(c(45)) &= t(35) \\
           &= 1.06(35) \\
           &= 37.10 \\
  c(t(45)) &= c(47.70) \\
           &= 47.70 - 10 \\
           &= 37.70
\end{work}

\begin{enumerate}[label=(\alph*), itemsep=4pt]
  \item Coupon first, then tax: \$\blank{2cm}
  \item Tax first, then coupon: \$\blank{2cm}
  \item The shopper saves \$\blank{1.6cm} by having the coupon rung up
        first.
\end{enumerate}

\boxguard[16]
\item \textbf{Taking functions apart.}

\begin{enumerate}[label=(\alph*), itemsep=6pt]
  \item Decompose $h(x) = \sqrt{4x - 7}$. \quad
        Inner: $g(x) = $ \blank{2.6cm} \quad Outer: $f(u) = $ \blank{2.2cm}
  \item In Lesson 1.4 you shifted a graph with $k(x) = f(x + 6)$. Write it as
        a composition: $k(x) = f(g(x))$ where $g(x) = $ \blank{2.4cm}
  \item Was that change made \textbf{inside} or \textbf{outside} $f$?
        \quad \blank{2.4cm}
\end{enumerate}

\item Let $f(x) = 3x - 2$ and $g(x) = x^2$. Which expression is
      $(f \circ g)(x)$?

\begin{enumerate}[label=\Alph*., itemsep=3pt]
  \item $3x^2 - 2$
  \item $(3x - 2)^2$
  \item $9x^2 - 12x + 4$
  \item $3x^3 - 2x^2$
\end{enumerate}

\end{enumerate}

\vspace{0.1in}

\boxguard
\begin{extensionbox}
The \textbf{identity function} is $I(x) = x$. Let $f(x) = 5x - 2$.

\smallskip
First check that $I$ really does nothing: $f(I(x)) = $ \blank{2.6cm} and
$I(f(x)) = $ \blank{2.6cm}.

\smallskip
Now the interesting question: is there a function $g$ with
$f(g(x)) = x$ --- a function that \textit{undoes} $f$? Try to find it, and
explain how you searched.

\vspace{0.05in}
\writelines{2}
\end{extensionbox}

\vspace{0.1in}

\boxguard
\begin{notesbox}{Preview of Lesson 1.6}
The extension asked for a function that undoes $f$. Lesson 1.6,
\textit{Inverse Functions}, gives that function a name and a notation:
$f^{-1}$. The test for it is a composition --- $f(f^{-1}(x)) = x$ --- which is
exactly the identity function you met today.
\end{notesbox}

\end{document}
