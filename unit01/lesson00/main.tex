%! Introduction to Functions and Change — Unit 1, Lesson 1.0 — Lesson Plan
\documentclass[10pt]{article}
\usepackage{precalculus-article}
\usepackage{precalculus-boxes}
\usepackage{graphicx}
\graphicspath{{images/}}
\newcommand{\TallMath}[1]{$\displaystyle #1\rule[-1.4em]{0pt}{3.2em}$}
\newcommand{\CourseName}{Precalculus}
\newcommand{\MeetingLength}{60 minutes}
\newcommand{\UnitNumberName}{Unit 1: Functions and Change \quad}
\newcommand{\LessonNumberName}{Lesson 1.0: Introduction to Functions and Change}

\begin{document}
\begin{center}
    {\Large\bfseries \CourseName \\
    {\small\bfseries \UnitNumberName \LessonNumberName}}
\end{center}

\begin{tcolorbox}[colback=lilac, colframe=plum, boxrule=0.9pt, arc=2mm,
    left=2mm, right=2mm, top=1.5mm, bottom=1.5mm]
    \textbf{Primary Objective:} Students will identify the input and output
    quantities in a real-world relationship and say which depends on the other,
    explain in everyday language what makes a relationship a \textit{function}
    (each input produces exactly one output), distinguish the allowed
    many-to-one case from the disqualifying one-to-many case, and read a
    pre-drawn graph or a table to say where a quantity is increasing,
    decreasing, or constant.
    \hfill\textit{\small (Unit introduction --- no CED topic)}
\end{tcolorbox}

\boxguard[22]
\begin{skillbox}[Lesson Flow --- Gradual Release (60 minutes)]{lilac}
\small
\renewcommand{\arraystretch}{1.25}
\begin{tabularx}{\linewidth}{@{} l c X @{}}
\textbf{Phase} & \textbf{Min} & \textbf{What students are doing, and where it lives} \\
\midrule
Warm-Up                       & 5  & Silent, individual spiral review --- warm-up sheet. \\
Hook                          & 3  & Whole-class discussion --- hook box atop the guided notes. \\
\textbf{I Do} --- model       & 10 & Watch and annotate while the teacher thinks aloud --- notes \S1. \\
\textbf{We Do} --- guided     & 18 & Fill the notes together, every blank cold-called --- notes \S2--\S3. \\
\textbf{You Do} --- independent & 14 & Work alone while the teacher circulates --- notes \S4 and the practice box. \\
Debrief                       & 5  & Whole-class share-out, the headline, the unit map --- notes \S5. \\
Exit Ticket                   & 5  & Individual, silent, collected. \\
\end{tabularx}

\smallskip
\textbf{If the clock slips:} the whole release lives in the guided notes today, so
there is no activity sheet to raid. Cut \textbf{practice item 3} (the runner) first
--- it is the extension, and the Debrief share-out covers it aloud anyway. Do not cut
the Debrief: it is where the many-to-one rule is said aloud for the third time, and
the exit ticket asks for it in writing.
\end{skillbox}

\renewcommand{\arraystretch}{1.6}
\begin{skillbox}[Priority Ideas \& Skills]{goldbox}
\begin{tabularx}{\linewidth}{@{}X X@{}}
\textbf{Priority Skills} &
\textbf{Key Understandings} \\
\midrule
Identify the input and output quantities in a contextual relationship
and decide which depends on which. &
\begin{itemize}[leftmargin=1.2em,itemsep=2pt,topsep=0pt]
  \item The input (independent) quantity is the one we choose or track;
        the output (dependent) quantity responds to it.
  \item Ordered pairs list input first, output second.
  \item The ``you-control'' test settles most cases in one question.
\end{itemize} \\
Decide informally whether a rule is a function, and justify the decision. &
\begin{itemize}[leftmargin=1.2em,itemsep=2pt,topsep=0pt]
  \item A function sends each input to \textit{exactly one} output ---
        a dependable machine.
  \item Many-to-one is allowed; one-to-many is not. The rule constrains
        each \textit{input}, never the outputs.
  \item Reversing the roles can turn a function into a non-function.
\end{itemize} \\
Read qualitative behavior from a pre-drawn graph or a table. &
\begin{itemize}[leftmargin=1.2em,itemsep=2pt,topsep=0pt]
  \item A graph tells a story: rising, falling, and flat stretches describe
        how the output changes as the input moves.
  \item A flat stretch means the output holds steady --- not that
        nothing is happening.
  \item Students interpret pre-drawn graphs; no freehand sketching.
\end{itemize} \\
\end{tabularx}
\end{skillbox}

\begin{skillbox}[{Vocabulary, Concepts, \& Theorems}]{greenbox}
\renewcommand{\arraystretch}{1.4}
\begin{tabularx}{\linewidth}{@{} >{\leavevmode\bfseries\color{plum}}p{0.24\linewidth} X @{}}
Quantity             & Something measurable that can change: time, height, cost, temperature. \\
Input                & The quantity we choose or track; listed first in an ordered pair. \\
Output               & The quantity that responds; listed second in an ordered pair. \\
Independent variable & Another name for the input quantity. \\
Dependent variable   & Another name for the output quantity --- it \textit{depends} on the input. \\
Function             & A rule that sends each input to exactly one output. (Informal today; notation in 1.1.) \\
Many-to-one          & Two or more inputs sharing one output. Allowed --- still a function. \\
One-to-many          & One input with two different outputs. Not allowed --- not a function. \\
Increasing / decreasing / constant & How an output behaves as the input grows: rising, falling, or holding steady. \\
\end{tabularx}
\end{skillbox}

\begin{skillbox}[Activate Prior Knowledge \& Spiral Review (5 min)]{lilac}
    Please see attached warm-up (spiral review).
    Skills reviewed: evaluating an algebraic expression at a value,
    solving a two-step linear equation,
    reading a value from a table and describing its trend,
    recognizing a point on a coordinate axis.
\end{skillbox}

\begin{skillbox}[Hook (3 min)]{lilac}
    \textbf{The trustworthy machine.} A vending machine and a claw machine both take
    your money. Press B4 on the vending machine and you get the same bag of chips
    \textit{every single time}; drop the claw and you get\ldots{} who knows.
    \begin{enumerate}[itemsep=2pt]
        \item Which machine can you \textit{trust}? Why?
        \item What would it mean for the vending machine to ``break the rules''?
    \end{enumerate}
    \textit{Discussion prompt:} mathematics reserves a special name for the
    trustworthy machine --- a \textit{function}: same input, same output, every time.
    Collect student examples of dependable and undependable ``machines'' from life.
    Do not define anything yet; the hook only needs to establish that
    \textit{dependability} is the property worth naming.
\end{skillbox}

\begin{skillbox}[Lesson --- I Do / We Do / You Do]{lilac}
\begin{multicols}{2}

\textbf{I DO --- Part 1: Quantities That Change Together} (10 min)

\textit{Guided notes \S1. Teacher works; students watch and annotate. Nothing is
cold-called in this phase.}
\begin{itemize}
  \item Name the unit's whole subject out loud: pairs of quantities linked so
        that one \textit{responds} to the other --- minutes of talk time and the
        phone bill; hours a candle burns and its height; time since a ball was
        dropped and its height.
  \item Model the completed first row of the notes table by \textit{narrating the
        reasoning}, not announcing the answer: ``I choose how many minutes I
        talk, so that is the input; the bill responds, so that is the output.''
  \item Do the candle row the same way --- still talking, still not asking.
        Students write what you write.
  \item Drop the synonyms in as you go: input = independent variable,
        output = dependent variable; ordered pairs are (input, output).
  \item Close the phase by naming the portable tool students will reuse all
        year: the ``you-control'' test.
\end{itemize}

\textbf{WE DO --- Part 2: What Is a Function? (First Look)} (10 min)

\textit{Guided notes \S2. Class fills the blanks together; cold-call every one.}
\begin{itemize}
  \item State it carefully once: a rule is a \textbf{function} when each input
        produces \textit{exactly one} output.
  \item Work the examples table by cold call --- student $\to$ ID number (yes);
        month $\to$ students born that month (no); number $\to$ its double (yes);
        shoe size $\to$ a student who wears it (no).
  \item Then the asymmetry, the hardest idea of the period: two inputs may share
        an output (both twins have the same birthday), but one input may never
        have two outputs. Say it in both directions, then make the class say it
        back before anyone writes.
  \item Keep it verbal --- notation is deliberately absent today.
\end{itemize}

\columnbreak

\textbf{WE DO --- Part 3: A Graph Tells a Story} (8 min)

\textit{Guided notes \S3. Still together, but hand over more of each answer.}
\begin{itemize}
  \item Project the pre-drawn bike-ride graph (distance from home vs.\ time):
        ride away, rest, ride back.
  \item Read it left to right as a story --- rising = moving away, flat =
        resting, falling = returning. Ask for the \textit{sentence} before you
        ask for the vocabulary word.
  \item Ask where the rider is at $t = 5$ with no formula available: the graph
        \textit{is} the information.
  \item Vocabulary in action: increasing, decreasing, constant. Insist that a
        flat stretch is not ``nothing happening.'' No sketching --- students
        only read and interpret.
\end{itemize}

\textbf{YOU DO --- Part 4 and Practice: A Table Tells the Same Story} (14 min)

\textit{Guided notes \S4 and the practice box. Students work alone; circulate
and say nothing a neighbor could say instead.}
\begin{itemize}
  \item The candle table makes the same three moves as the graph. The point is
        that the representation changed and the reading did not --- students
        should reach that themselves.
  \item The hour-2-to-3 flat stretch is the table's version of segment B; the
        notes ask for that connection directly.
  \item Then the practice box, which is the whole independent phase: the
        movie-theater input/output item, the quiz-score table whose repeated 80
        is the many-to-one payoff, and the runner item, where the same distance
        appears at two different times.
  \item Item 3 is the deepest idea of the period --- distance is a function of
        time, but time is not a function of distance. Let students reach it;
        it is what the Debrief share-out closes on.
  \item Notes \S5, the unit map, is \textit{not} read here --- it belongs to the
        Debrief.
\end{itemize}
\end{multicols}
\end{skillbox}

\boxguard
\begin{skillbox}[Active Monitoring]{redbox}
Circulate during the \textbf{You Do} phase --- the fourteen minutes where
students are working without you --- and check:
\begin{itemize}
  \item \textbf{Common error:} swapping input and output (``the bill determines the
        minutes''). Cold-call: ``Which quantity do \textit{you} control?''
  \item \textbf{Common error:} rejecting many-to-one rules as non-functions.
        Ask: ``Two students both wear size 9. Does student $\to$ shoe size stop
        being a function?'' (No --- each student still has one size.)
  \item \textbf{Common error:} stating the rule about outputs (``each output has
        one input''). Insist on the direction: the rule constrains each
        \textit{input}.
  \item \textbf{Graph reading:} students describing a flat segment as ``nothing
        happening.'' Push for precision: the input (time) is still moving; the
        output is constant.
  \item \textbf{Cold-call:} ``Give me a rule from your life that IS a function.
        Now break it so it isn't.''
\end{itemize}
\end{skillbox}

\begin{skillbox}[Differentiation --- During You Do (14 min)]{redbox}
There is no separate activity sheet: students work the notes' practice box alone
while you circulate. Differentiate by \textit{where you stand}, not by handing
out different paper.

\begin{itemize}
  \item \textbf{Support.} Sit with a struggling student on \textbf{item 1} and ask
        one question only: ``Which quantity do \textit{you} control?'' If the
        input/output split is not there, item 2 will not land either. Have them
        say the answer aloud before writing it.
  \item \textbf{On level.} Let the class run at \textbf{item 2} unaided. The
        repeated 80 is meant to feel wrong for a moment --- do not rescue it.
        Wait until a student says ``but two hours and three hours both give
        80,'' then ask the room whether that breaks the rule.
  \item \textbf{Extend.} \textbf{Item 3} is the finisher's question and needs no
        setup from you: the runner is 0.3 km out at two different times. A
        student who resolves both halves --- distance \textit{is} a function of
        time, time is \textit{not} a function of distance --- presents it in the
        Debrief. That asymmetry is the same one exit-ticket item 2 tests.
\end{itemize}
\end{skillbox}

\boxguard[20]
\begin{skillbox}[Debrief (5 min)]{redbox}
Whole class, teacher-led, packets still open. Three moves, in order:
\begin{enumerate}[itemsep=3pt]
  \item \textbf{Share out the You Do} (2 min) --- one answer per practice item,
        not a full review. Item 1: the input and output for the movie theater.
        Item 2: why the repeated 80 leaves the score a function of hours
        studied. Item 3: the 0.3\,km puzzle. If a student settled ``time is
        \textit{not} a function of distance,'' this is where they present it ---
        and it is the only exposure the rest of the class gets to it.
  \item \textbf{Name the headline} (1 min) --- say it, then make the class say
        it back: \textit{a function sends each input to exactly one output; two
        inputs may share an output, but one input may never have two.} This is
        the third and last time it is said aloud today, and the exit ticket asks
        for it in writing two minutes later.
  \item \textbf{Read the unit map} (2 min) --- guided notes \S5 is read-only.
        Read it aloud and do not add to it. Students should leave knowing that
        1.1 gives functions their notation and that the rest of the unit is
        about how quantities change together.
\end{enumerate}
\textbf{Do not} re-teach here, take new questions, or let the exit ticket start
early. A debrief that runs long eats the only individual data collected today.
\end{skillbox}

\begin{skillbox}[Individual Work \& Assessment (5 min)]{redbox}
\textbf{Exit Ticket} (final 5 minutes, individual, silent, collected):
\begin{enumerate}
  \item Identify the input and output quantities for an oven preheating.
  \item Decide whether ``each car $\to$ its license plate number'' is a function,
        with one sentence of reasoning.
  \item From a small table of plant heights, name the interval where the height is
        constant and the interval where it is increasing.
\end{enumerate}
\textbf{Assessment note:} Item 2 is the heart of the lesson --- look for the phrase
``exactly one'' (or an equivalent) in the reasoning, not just a circled yes. Sort
tickets by item 2 and revisit the many-to-one vs.\ one-to-many distinction in the
1.1 warm-up if more than a handful miss it.
\end{skillbox}

\begin{skillbox}[Reinforcement \& Extension]{goldbox}
\textbf{Homework --- DeltaMath.} This lesson ships no printed homework; assign the
DeltaMath set and have students record its name, due date, and score in the
homework row of the packet cover. Target coverage: identifying input and output
in a context, function-or-not judgments including at least one many-to-one case,
and reading increasing / decreasing / constant behavior off a table or a
pre-drawn graph.

\textbf{Extension (optional):} Two different vending-machine buttons dispense the
same snack --- is the machine still a function? Students explain why many-to-one
does not break the rule. Assign it to anyone who did not reach practice item 3.

\textbf{Preview:} Next lesson (1.1, Function Fundamentals) functions get their
notation --- $f(x)$ --- along with domain, range, and evaluating; in 1.2 we study
how two quantities change \textit{in tandem}.
\end{skillbox}

\begin{teachernote}[Warm-Up]
Five minutes, silent start, no calculators. The four items are pure Algebra 1
maintenance --- this is also your first diagnostic look at the class. A student
stuck on item 1 or 2 needs watching when evaluation returns with $f(x)$ notation
in 1.1.
\end{teachernote}

\boxguard[11]
\begin{teachernote}[Guided Notes]
Keep Part 2 verbal and fast --- the examples table does the work, and formal
notation is deliberately absent (it arrives in 1.1; do not front-load it today).
The many-to-one / one-to-many table underneath it is the block worth slowing for:
students who can only state the rule in one direction will misread the exit
ticket. The bike-ride and candle figures are pre-drawn; students annotate and fill
blanks only. Section 5 has moved out of the notes block entirely ---
it is now read aloud in the Debrief, so do not spend notes time on it.

The practice box is the entire independent phase and it carries fourteen minutes,
so treat it as the lesson's third act rather than a leftover. Item 3 is the
deepest idea of the day --- the runner's distance is a function of time, but time
is \textit{not} a function of distance --- and it needs no algebra, only patience.
If the clock slips, cut item 3 and let the Debrief share-out carry it; cut item 2
only as a last resort, since the repeated 80 is the many-to-one payoff the exit
ticket tests.
\end{teachernote}

\begin{teachernote}[Debrief]
Guard these five minutes --- they are the first thing the clock takes and the
last thing you should surrender. The share-out is deliberately one answer per
practice item so that a student who never reached item 3 still hears the runner
puzzle; it is the only exposure they get to it. Do not let the unit map turn into a preview
lecture: two minutes of reading aloud, whose only job is to tell students that
today's two words are the spine of everything that follows.
\end{teachernote}

\begin{teachernote}[Exit Ticket]
Collected, completion credit only (``mistakes happen, blanks don't''). Grade item 2
with a one-second scan for ``exactly one'' reasoning --- that phrase predicts
readiness for the formal definition in 1.1.
\end{teachernote}

\begin{teachernote}[Homework --- DeltaMath]
Homework is DeltaMath unless a lesson ships a printed homework component; 1.0
does not. Put the assignment name and due date on the board while students fill
the cover row, so the packet carries its own record of what was assigned. Read
the DeltaMath report by item type rather than by overall score: a class that
clears the input/output questions but stalls on function-or-not needs the 1.1
warm-up spent on many-to-one, not on notation. The extension answer
(many-to-one is fine) mirrors practice item 3, so students who reached it have
already met the idea and can be pointed at the extension as review rather than
new work.
\end{teachernote}
\end{document}
